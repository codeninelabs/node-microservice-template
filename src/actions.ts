import grpc from "grpc";
// import {IReportsServer, ReportsService} from "./proto/service_grpc_pb";
// import {PingReply, PingRequest, Reply, Request, User} from "./proto/service_pb";

// class ServicesHandler implements IReportsServer {
//     health = (call: grpc.ServerUnaryCall<PingRequest>, callback: grpc.sendUnaryData<PingReply>): void => {
//         const reply: PingReply = new PingReply();
//         reply.setStatus(call.request.toString());
//         callback(null, reply);
//     };
// }

export default {
//     service: ReportsService,                // Service interface
//     handler: new ReportsHandler(),          // Service interface definitions
};
